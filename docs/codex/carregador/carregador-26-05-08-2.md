# Contexto
Você é um desenvolvedor Senior em dart / Flutter
Leia o resumo do prompt anterior, se houver, e continue a evolução.

## Arquivos
@file:carregador_widget.dart
`lib/features/carregador`

## Regras
- vazio

## Restrições
- Não reescreva os arquivos inteiros quando for executar a tarefa, apenas altere as linhas necessárias

## Entregáveis
1. escrever estrutura da feature em arvore-feature-carregador.md;
2. salvar um resumo da execução com nome datado e sufixo `-resumo`;
3. rodar o script @file:base-prompt-tarefas.md com o [NOME_DA_PASTA] já definido;

# Descrição
- Descrição da arquitetura da `feature carrador`

## Objetivo
- Faça duas arvores da estrutura a `feature carregador` :
    1. uma com os arquivos e pastas presentes atualmente
    2. uma com os arquivos e pastas de como ficará após adicionar `CarregadorPage()` e `CarregadoresPageViewModel()` que utilizará @file:carregador_widget.dart 
